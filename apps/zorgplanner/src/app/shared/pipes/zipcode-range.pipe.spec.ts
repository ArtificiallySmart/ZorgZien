import { ZipcodeRangePipe } from './zipcode-range.pipe';

describe('ZipcodeRangePipe', () => {
  it('create an instance', () => {
    const pipe = new ZipcodeRangePipe();
    expect(pipe).toBeTruthy();
  });
});
