import { FteToHoursPipe } from './fte-to-hours.pipe';

describe('FteToHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new FteToHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
